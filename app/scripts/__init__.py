"""Operational commands for the GEO Shopping Lab backend.

These modules are invoked explicitly with ``python -m``.  They never run as
part of the FastAPI request lifecycle, which keeps long catalog imports from
blocking participant traffic.
"""
